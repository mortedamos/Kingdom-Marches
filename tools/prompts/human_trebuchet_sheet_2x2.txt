Create a single image containing a 2x2 grid of four equal square
panels (top-left, top-right, bottom-left, bottom-right), separated by
a thin visible gap so they can be cut apart afterward. This is a
4-frame idle animation sprite sheet for a single video game siege
engine — a machine, not a character. The SAME trebuchet must appear in
all four panels — identical identity, art style, colors, palette,
proportions, and camera angle/distance. Only small details of tension
and motion change, panel to panel, as if the machine is creaking
gently under its own weight while at rest.

Art style (applies to all four panels): a cel-shaded outline style —
thick, clean black outlines around the silhouette and major interior
linework, sharp details, cel-shaded flat colors with a limited palette
(2-3 hard-edged tonal steps per surface, no soft airbrushed gradients).
Muted, desaturated, earthy fantasy palette (weathered wood brown, dull
iron gray, faded tan rope). Elevated 3/4 top-down "strategy game" view,
the same angle and framing used for other units in this game.

Subject (applies to all four panels): a large wooden human trebuchet —
a clear upgrade from a simple catapult: a taller A-frame timber tower,
a long pivoting throwing beam with a heavy iron-bound counterweight box
on the short end and a sling holding a single round stone projectile
on the long end, all mounted on a sturdy wheeled timber base. Bigger,
taller, and more mechanically complex-looking than a basic catapult. A
muted purple (#8e44ad) accent may appear only as a small detail, such
as a painted trim stripe on the frame — never as the dominant color of
the wood or iron. Minimal ornamentation — no elaborate carvings, a
plain practical war machine. The trebuchet's overall shape, wheel
position, and frame stay the same in every panel — this is the same
machine, not a different pose or angle.

Panel 1 (top-left) — neutral resting pose: the throwing beam and
counterweight held at a low resting angle, ropes and beam settled, no
visible motion.

Panel 2 (top-right) — a clear, obvious shift in tension: the
counterweight and throwing beam creak and settle a noticeably
different amount — the beam sits at a clearly different angle than
panel 1, the counterweight box shifted — an obviously different,
noticeable change at a glance, but a natural amount of settling, not
the trebuchet firing or mid-launch.

Panel 3 (bottom-left) — settling the other way: the throwing beam and
counterweight shift to another clearly different resting angle —
noticeably different from both panel 1 and panel 2. This is NOT a
mirrored or horizontally flipped image — the trebuchet's wheels,
frame, and overall silhouette stay consistent with the other panels,
only the beam/counterweight position changes.

Panel 4 (bottom-right) — settling, mid-transition: the throwing beam
and counterweight settle back partway between panel 3 and panel 1,
tension easing but not fully back to panel 1's resting state yet.

Every panel: the trebuchet is centered within its panel at the same
scale, same camera distance, and same padding, the full machine
visible, on a single flat, solid, saturated magenta (#FF00FF)
background — completely uniform, no gradient, no shadow, no vignette,
and no ground/terrain rendered under the wheels (the trebuchet itself
sits in the flat magenta field).

Do not include any text, panel labels, numbers, watermark, logo, UI
elements, sparkle/star decorations, or border anywhere in the image.
Overall image should be a square 1:1 aspect ratio.
