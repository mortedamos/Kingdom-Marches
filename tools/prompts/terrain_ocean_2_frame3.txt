This is the reference image: a single square deep-ocean terrain tile
viewed from directly above, with TWO soft-edged, gently curved,
roughly-horizontal wave-crest bands (lighter teal-blue highlight with a
thin soft pale foam highlight line along each ridge) on a deep blue
base fill, edge-to-edge with no border — one band nearing the bottom
edge, one near the middle of the tile. Produce frame 3 of a 4-frame
rolling wave animation loop: an updated version of this EXACT same
image — same base color, same overall style, same soft gradient
blending, same lack of any border or outline around the tile edge —
but shift BOTH wave-crest bands further downward (the band nearest the
bottom edge should now be fading out as it exits past the bottom edge,
lower opacity/thinner; the band that was near the middle should now be
in the lower third). Their shape, width, and softness stay consistent
with the reference, only their vertical position moves. Do not add any
new elements, do not add a black outline or border anywhere, do not
change the base color or the framing/zoom level. The motion should be
clearly visible frame-to-frame, not barely noticeable.
