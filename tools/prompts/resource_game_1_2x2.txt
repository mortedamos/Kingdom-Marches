Create a single image containing a 2x2 grid of four equal square
panels (top-left, top-right, bottom-left, bottom-right), separated by
a thin visible gap so they can be cut apart afterward. This is a
4-frame idle animation sprite sheet for a small game-map tile resource
icon — a single deer, representing a "Game" resource on a forest/plains
tile. The SAME deer must appear in all four panels — identical
identity, art style, colors, palette, proportions, and camera
angle/distance. Only its pose changes, panel to panel.

CRITICAL: there is exactly ONE deer in this image, appearing exactly
once per panel. Each of the 4 panels contains a single solitary
animal — never two, never a duplicate, twin, or mirrored copy standing
side by side in the same panel.

Style: this is a small tile-resource overlay icon, not a full
character illustration — it sits on top of a terrain tile, underneath
the player's units, and must not visually compete with unit art. Cel-
shaded flat colors with a limited, muted, desaturated earthy palette
(warm browns, tans, soft olive), 2-3 hard tonal steps, no soft
gradients. IMPORTANT outline rule, different from normal game units:
do NOT use a thick black outline. Instead use a THIN outline in a
muted, dark shade of the deer's own brown/tan hue family — just enough
to separate the silhouette from the background, never pure black.
Keep interior linework minimal — one small dark line for the eye, one
or two lines suggesting the leg/body divisions — not detailed fur
texture or heavy cross-hatching. One or two bold, simple shapes only;
this icon renders very small on screen, so fine detail won't survive
downscaling and should not be attempted.

Subject (applies to all four panels): a small woodland deer (doe, no
antlers), warm tan-brown coat with a pale cream belly and throat, short
tail, slender legs. Standing in a calm, alert grazing/resting posture.

Framing: top-down flattened "tile icon" view (a slightly elevated
top-down angle is fine, matching a small overlay detail, NOT the full
elevated 3/4 diorama angle used for full-size units), the deer centered
in each panel filling the frame at roughly 10-15% padding on all sides
— the same fill amount as a normal game unit portrait, NOT a small
icon floating in a mostly-empty frame. Any relative size difference
between this and other tile resources is handled separately at render
time, not by leaving extra padding here. Full body visible.

Panel 1 (top-left) — neutral base pose: standing calm, head facing
forward, ears up.

Panel 2 (top-right) — alert, moderate motion: head turns a clear,
obvious amount to one side as if looking around — immediately
noticeable at a glance, not a subtle glance. Ears may swivel with the
motion.

Panel 3 (bottom-left) — alert, looking the other way: head turns a
clear, obvious amount toward the other direction from panel 2 — the
same deer in the same default stance as panel 1, simply turning to
look the opposite way. This is NOT a mirrored or horizontally flipped
image — body orientation and legs stay the same as every other panel.

Panel 4 (bottom-right) — settling, mid-transition: head turning back
toward facing forward, partway between panel 3 and panel 1, perhaps a
small downward dip as if about to graze.

Every panel: the deer is centered within its panel at the same scale,
same camera distance, and same padding, full body visible, on a single
flat, solid, saturated magenta (#FF00FF, bright hot pink, not a duller
or darker mauve) background — completely uniform, no gradient, no
shadow, no vignette.

Do not include any text, panel labels, numbers, watermark, logo, UI
elements, sparkle/star decorations, or border anywhere in the image.
Overall image should be a square 1:1 aspect ratio.
