This is the reference image: a single square plains terrain tile.
Produce an updated version of this EXACT same image — same green fill
color (#6EA54A), same overall composition, same edge-to-edge fill with
no border or outline of any kind — with only ONE tiny change: nudge the
small grass-tuft marks a few pixels from their current positions, as if
a light breeze moved them slightly. Keep the same number of tufts, same
size, same dark green color, same simple shape style, same black
outline stroke weight. Do not change the background green color, do not
add any new elements, do not add a border/outline/frame anywhere, do
not change the framing or zoom level. This is frame 2 of a 2-frame idle
animation loop, so the change must be subtle — barely noticeable, not a
dramatic shift.
