Create a single image containing a 2x2 grid of four equal square
panels (top-left, top-right, bottom-left, bottom-right), separated by
a thin visible gap so they can be cut apart afterward. This is a
4-frame idle animation sprite sheet for a small game-map tile
enhancement icon — a "Ruin" tile feature: a weathered stone archway
entrance built into a rocky hillside, leading into a pool of solid
black shadow/darkness, like the entrance to a fantasy dungeon.

CRITICAL — the archway structure itself does NOT move or animate at
all: the stone arch, the hillside rock around it, the black shadow
opening, and every crack must be PIXEL-FOR-PIXEL IDENTICAL in all four
panels, as if the exact same structure image were copied into all four
panels and only small extra details were added on top. Do not lean,
tilt, shift, resize, or redraw the archway differently between panels
— treat the stone structure as a fixed, unmoving background element.
The ONLY things that are allowed to change panel to panel are: a
couple of small weed/grass tufts at the base of the archway swaying
gently, and a few small pale dust motes drifting near the dark
opening. Everything else must stay exactly the same.

CRITICAL: there is exactly ONE archway in this image, appearing once
per panel, in the exact same position, shape, and size in every panel.

Style: this is a small tile-overlay icon, not a full scene
illustration — it sits on top of a terrain tile, underneath the
player's units, and must not visually compete with unit art. Cel-
shaded flat colors with a limited, muted, desaturated palette
(weathered pale gray-tan stone, muted green weeds, solid black shadow
in the opening), 2-3 hard tonal steps, no soft gradients. IMPORTANT
outline rule, different from normal game units: do NOT use a thick
black outline on the stone silhouette. Instead use a THIN outline in a
muted, dark gray shade for the stone, and dark green for the weeds —
just enough to separate shapes, never a heavy black line (the solid
black shadow INSIDE the archway opening is a fill, not an outline, and
is fine as-is). Keep interior linework minimal — a few simple lines
suggesting cracked stone blocks, not detailed texture or heavy
cross-hatching. This icon renders small on screen, so fine detail
won't survive downscaling and should not be attempted.

Subject (applies to all four panels): a squat, weathered stone archway
entrance set into a low rocky mound/hillside, like the mouth of an old
dungeon or barrow. The archway opening is filled with solid flat black
(pure shadow, no visible interior detail). The surrounding stonework
is cracked and weathered, with a couple of small grass/weed tufts
growing at the base.

Framing: top-down flattened "tile icon" view (a slightly elevated
top-down angle is fine, NOT the full elevated 3/4 diorama angle used
for full-size units), the archway centered in each panel filling the
frame at roughly 10-15% padding on all sides — the same fill amount as
a normal game unit portrait, NOT a small icon floating in a mostly-
empty frame. This resource renders a little larger on the tile than
other resource icons, but that relative size difference is handled
separately at render time, not by leaving extra or less padding here
— keep the same ~10-15% padding convention regardless.

Panel 1 (top-left) — neutral base pose: weeds upright, no dust
visible. The archway structure exactly as described above.

Panel 2 (top-right) — ambient detail only: the weeds lean a clear,
obvious amount to one side as if stirred by a breeze, and a couple of
small pale dust motes appear drifting near the dark opening —
immediately noticeable at a glance, not a subtle glance. The archway
structure itself is UNCHANGED, pixel-for-pixel identical to panel 1.

Panel 3 (bottom-left) — ambient detail continues: the weeds lean a
clear, obvious amount toward the other direction from panel 2, and the
dust motes have drifted to a new position — a genuinely redrawn
moment, not a horizontally flipped copy of panel 2. The archway
structure itself is UNCHANGED, pixel-for-pixel identical to panel 1.

Panel 4 (bottom-right) — settling: weeds straightening back toward
upright, dust motes fading out, partway between panel 3 and panel 1.
The archway structure itself is UNCHANGED, pixel-for-pixel identical
to panel 1.

Every panel: the archway is centered within its panel at the same
scale, same camera distance, and same padding, on a single flat,
solid, saturated magenta (#FF00FF, bright hot pink, not a duller or
darker mauve) background — completely uniform, no gradient, no shadow,
no vignette (the black shadow filling the archway opening itself is
the one exception, since that is part of the subject, not the
background).

Do not include any text, panel labels, numbers, watermark, logo, UI
elements, sparkle/star decorations, or border anywhere in the image.
Overall image should be a square 1:1 aspect ratio.
