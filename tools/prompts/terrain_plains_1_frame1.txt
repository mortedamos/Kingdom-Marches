Create a single square game terrain tile image, NOT a grid, NOT
multiple panels — one single flat square image only.

Game asset: Plains terrain tile, standard variant.

Style: a cel-shaded outline style — thick, clean black outlines around
major shapes, cel-shaded flat colors with a limited palette (2-3
hard-edged tonal steps per surface, no soft airbrushed gradients, no
photographic texture). Extremely simple: this tile is rendered very
small on screen and sits behind game units, so it must read as one or
two bold, flat color shapes, not a detailed illustration. Absolutely no
fine linework, no small repeated texture marks covering the whole tile,
no gradient shading.

Subject: an open grassland plain. The background fill MUST be exactly
this flat solid color: hex #6EA54A (a fresh mid grass green) — match
this color precisely, do not substitute a similar green. On top of that
flat fill, add 3-5 small, simple, slightly darker green grass-tuft
marks scattered across the tile — bold simple shapes with a thin black
outline, consistent stroke weight on every tuft. No flowers, no rocks,
no trees, no dirt patches, no path lines.

Framing: top-down flattened tile view (camera looking straight down,
NOT an elevated 3/4 angle), square 1:1 composition. The green color
fill extends completely edge-to-edge across the entire image with zero
padding or margin.

CRITICAL: there is no black outline, border, frame, or line drawn
around the edge of the image — the grass-green fill is the only content
and it must reach all the way to the literal pixel edge on all four
sides, corner to corner. Do not draw a darker strip, band, shadow,
vignette, or any other edge treatment anywhere near any edge — color
brightness and saturation must be perfectly uniform across the entire
image. This is a single seamless texture tile meant to butt up directly
against identical neighboring tiles with zero visible seam, so nothing
in this image may mark or outline its own boundary.

Do not include any text, labels, numbers, watermark, logo, UI elements,
or border anywhere in the image. Overall image should be a square 1:1
aspect ratio.
