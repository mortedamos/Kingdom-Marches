Create a single square game terrain tile image, NOT a grid, NOT
multiple panels — one single flat square image only.

Game asset: Desert terrain tile, alternate variant (a cluster of small
sand-colored rocks instead of ripple bands, for visual variety across a
map). This is a STATIC tile (no animation), so it only needs to exist
as one single frame.

Style: this is a TERRAIN tile, not a character or object — it must
recede visually behind game units, which use bold black outlines and
sharp cel-shaded flat colors. The ground texture itself uses soft,
smoothly blended color gradients with no hard black outlines. This tile
is also rendered very small on screen, so it must read as one or two
bold, simple shapes — not a detailed illustration.

Subject: open desert sand, viewed from directly above (top-down). The
base fill is this flat solid color: hex #CDB57E (a warm sandy tan). On
top of that base fill, add ONE soft-edged dune-ripple band sweeping
across a corner of the tile (like the shadow line of a wind-blown sand
ripple), a subtle, softly-blended slightly darker/warmer tan (hex
#B89A63) that fades smoothly into the base color on both sides — NO
hard edge, NO outline of any kind, just soft gradient blending.
Additionally, add a small cluster of 2-3 simple rounded rock shapes
(flat muted gray-tan fill, hex #A69572) with a normal thin BLACK
outline (like the small foreground grass-tuft marks used on plains
tiles — rocks are a foreground detail, not part of the ground shading),
positioned off-center with a clear margin from every edge of the tile.
Keep the rocks simple and bold, not detailed.

Framing: top-down flattened tile view (camera looking straight down,
NOT an elevated 3/4 angle), square 1:1 composition. The base color fill
extends completely edge-to-edge across the entire image with zero
padding or margin — only the rock cluster stays clear of the edges, per
above.

CRITICAL: there is no black outline, border, frame, or line drawn
around the edge of the image, and there are no corner brackets, crop
marks, tick marks, or any small graphic mark of any kind in any of the
four corners or along any edge — completely plain and unmarked at the
tile boundary. There is also no vignette or corner darkening — overall
brightness stays even corner to corner. This is a single seamless
texture tile meant to butt up directly against identical neighboring
tiles with zero visible seam, so nothing in this image may mark or
outline its own boundary in any way.

Do not include any text, labels, numbers, watermark, logo, UI elements,
or border anywhere in the image. Overall image should be a square 1:1
aspect ratio.
