Create a single image containing a 2x2 grid of four equal square
panels (top-left, top-right, bottom-left, bottom-right), separated by
a thin visible gap so they can be cut apart afterward. This is a
4-frame idle animation sprite sheet for a single video game siege
engine — a machine, not a character. The SAME catapult must appear in
all four panels — identical identity, art style, colors, palette,
proportions, and camera angle/distance. Only small details of tension
and motion change, panel to panel, as if the machine is creaking
gently under its own tension while at rest.

Art style (applies to all four panels): a cel-shaded outline style —
thick, clean black outlines around the silhouette and major interior
linework, sharp details, cel-shaded flat colors with a limited palette
(2-3 hard-edged tonal steps per surface, no soft airbrushed gradients).
Muted, desaturated, earthy fantasy palette (weathered wood brown, dull
iron gray, faded tan rope). Elevated 3/4 top-down "strategy game" view,
the same angle and framing used for other units in this game.

Subject (applies to all four panels): a SIMPLE, ICONIC wooden catapult
— deliberately simplified geometry, not a detailed engineering
replica. Just three structural pieces: (1) a sturdy A-frame stand made
of a small number of thick, plain timber beams — no extra diagonal
cross-bracing beyond the basic A-shape, (2) two large wheels at the
base, one visible on each side, (3) a single thick wooden throwing arm
pivoting on a simple visible axle near the top of the frame, ending in
one large, clearly-shaped round basket or cupped bowl holding one big
round stone. Rope is minimal — just a couple of simple lines anchoring
the arm, not a thick twisted bundle or tangle. No winch, no crank, no
counterweight box, no extra mechanical detail beyond these three
pieces. A muted purple (#8e44ad) accent may appear only as a small
detail, such as a painted trim stripe on the frame — never as the
dominant color of the wood or iron. This should read as a simple, bold,
instantly-recognizable catapult silhouette at a glance — think a
children's-book illustration of a catapult, not a mechanically-accurate
diagram. The catapult's overall shape, wheel position, and frame stay
the same in every panel — this is the same machine, not a different
pose or angle.

CRITICAL: this catapult has exactly ONE throwing arm and exactly ONE
basket/bowl holding exactly ONE stone projectile — in every single
panel, no exceptions. Never draw a second throwing arm, a second
basket, or an empty basket. If a panel doesn't clearly show the one
throwing arm with its one loaded basket, the panel is wrong — the arm
and its projectile must always be visible and present, just at a
different angle or tension per panel, never missing and never
duplicated.

Each panel below shows the SAME single throwing arm, with its ONE
loaded basket, simply drawn at a different fixed angle. Do not think of
these as a before/after transition or a settling motion — each panel is
an independent static snapshot of the one arm at one angle. Never draw
the arm at two angles, or the basket in two places, within the same
panel.

Panel 1 (top-left): the throwing arm is angled low, close to
horizontal, its basket near the bottom of the frame.

Panel 2 (top-right): the throwing arm is angled high, closer to
vertical, its basket near the top of the frame — clearly, obviously
higher than panel 1.

Panel 3 (bottom-left): the throwing arm is at a middle angle, between
panel 1's low position and panel 2's high position — clearly distinct
from both. This is NOT a mirrored or horizontally flipped image — the
catapult's wheels, frame, and overall silhouette stay consistent with
the other panels, only the single arm's angle changes.

Panel 4 (bottom-right): the throwing arm is angled high again, similar
to panel 2 but at a slightly different angle from it — still clearly
higher than panel 1's low position.

Every panel: the catapult is centered within its panel at the same
scale, same camera distance, and same padding, the full machine
visible, on a single flat, solid, saturated magenta (#FF00FF)
background — completely uniform, no gradient, no shadow, no vignette,
and no ground/ terrain rendered under the wheels (the catapult itself
sits in the flat magenta field).

Do not include any text, panel labels, numbers, watermark, logo, UI
elements, sparkle/star decorations, or border anywhere in the image.
Overall image should be a square 1:1 aspect ratio.
