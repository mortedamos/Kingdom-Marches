This is the reference image: a single square swamp terrain tile viewed
from directly above, showing one dark, murky water pool with a muted
outline and soft gradient shading, contained well within the tile
boundary, with a few reed/marsh-grass tufts nearby. Produce an updated
version of this EXACT same image — same base color, same pool
shape/position/size, same muted outline, same gradient shading, same
overall composition, same edge-to-edge base fill with no border around
the tile — with only ONE tiny change: a very subtle shift in the
reed/marsh-grass tufts' positions, as if a light breeze moved them
slightly (the pool shape itself stays completely the same). Do not
change the background color, do not add any new elements, do not add a
black outline or border anywhere, do not change the framing or zoom
level. This is frame 2 of a 2-frame idle animation loop, so the change
must be subtle — barely noticeable, not a dramatic shift.
