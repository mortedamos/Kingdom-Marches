Create a single square game terrain tile image, NOT a grid, NOT
multiple panels — one single flat square image only.

Game asset: Desert terrain tile, standard variant. This is a STATIC
tile (no animation), so it only needs to exist as one single frame.

Style: this is a TERRAIN tile, not a character or object — it must
recede visually behind game units, which use bold black outlines and
sharp cel-shaded flat colors. Terrain does the OPPOSITE: soft, smoothly
blended color gradients, with no hard black outlines on the ground
texture itself. This tile is also rendered very small on screen, so it
must read as one or two bold, simple shapes — not a detailed
illustration.

Subject: open desert sand, viewed from directly above (top-down — sand
ripples are a flat ground texture, not a raised landform). The base
fill is this flat solid color: hex #CDB57E (a warm sandy tan). On top
of that base fill, add 2-3 gently curved, soft-edged dune-ripple bands
sweeping across the tile (like the shadow lines of wind-blown sand
ripples), each a subtle, softly-blended slightly darker/warmer tan
(hex #B89A63) that fades smoothly into the base color on both sides —
NO hard edge, NO outline of any kind on these ripple bands, just soft
gradient blending, like a shallow shadow. Keep the ripple bands
gentle and spaced apart, not a busy repeating pattern. No cacti, no
rocks, no bones, no path lines — just the sand and its soft ripple
shading.

Framing: top-down flattened tile view (camera looking straight down,
NOT an elevated 3/4 angle), square 1:1 composition. The base color fill
extends completely edge-to-edge across the entire image with zero
padding or margin.

CRITICAL: there is no black outline, border, frame, or line drawn
around the edge of the image, and there are no corner brackets, crop
marks, tick marks, or any small graphic mark of any kind in any of the
four corners or along any edge — completely plain and unmarked at the
tile boundary. There is also no vignette or corner darkening — overall
brightness stays even corner to corner. This is a single seamless
texture tile meant to butt up directly against identical neighboring
tiles with zero visible seam, so nothing in this image may mark or
outline its own boundary in any way.

Do not include any text, labels, numbers, watermark, logo, UI elements,
or border anywhere in the image. Overall image should be a square 1:1
aspect ratio.
