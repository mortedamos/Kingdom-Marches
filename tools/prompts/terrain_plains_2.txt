Create a single image containing a 1x2 grid of two equal square panels
(left, right), separated by a thin visible gap so they can be cut apart
afterward. This is a 2-frame idle animation tile sheet for a single
terrain tile in a top-down strategy game. Both panels show the exact
same tile — identical content, colors, palette, framing, and camera
angle — with only a very subtle idle shift between them (like a light
breeze moving a few grass blades), not a different tile.

Game asset: Plains terrain tile, alternate variant (sparser, more open
grassland than the standard variant, for visual variety across a map).

Style: a cel-shaded outline style — thick, clean black outlines around
major shapes, cel-shaded flat colors with a limited palette (2-3
hard-edged tonal steps per surface, no soft airbrushed gradients, no
photographic texture). Extremely simple: this tile is rendered very
small on screen and sits behind game units, so it must read as one or
two bold, flat color shapes, not a detailed illustration. Absolutely no
fine linework, no small repeated texture marks covering the whole tile,
no gradient shading.

Subject: an open grassland plain, the EXACT SAME mid grass green flat
color fill as a standard plains tile (a fresh, natural green — not
olive, not yellow-green) — color consistency with other plains tiles is
critical since they sit directly adjacent on the map. This variant has
only 1-2 small, simple, slightly darker green grass-tuft marks total
(sparser than a denser plains tile), placed off-center, otherwise
completely bare and open. No flowers, no rocks, no trees, no dirt
patches, no path lines.

Framing: top-down flattened tile view (camera looking straight down,
NOT an elevated 3/4 angle), square 1:1 composition per panel. The green
color fill extends completely edge-to-edge across the entire panel with
zero padding or margin.

CRITICAL: there is no black outline, border, frame, or line drawn
around the edge of either panel — the grass-green fill is the only
content and it must reach all the way to the literal pixel edge of each
panel on all four sides. Do not draw a darker strip, band, shadow, or
any other edge treatment along the bottom (or any) edge of a panel —
the ONLY separation between the two panels is the thin neutral gap
between them described above, nothing is drawn on the tile content
itself to mark its boundary. There is also no vignette or corner
darkening — color brightness and saturation must be perfectly uniform
across the entire panel, corner to corner, edge to edge, since this
tile needs to butt up seamlessly against identical tiles with zero
visible seam in any direction, including top and bottom, not just left
and right.

Do not include any text, panel labels, numbers, watermark, logo, UI
elements, or border anywhere in the image. Overall image should be a
2:1 aspect ratio (two square panels side by side).
