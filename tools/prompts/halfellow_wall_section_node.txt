Game asset: Wall Segment, junction/hedge mound (race: Halfellow, category: wall).

A single game asset in a cel-shaded outline style: thick, clean black
outlines around the silhouette and major interior linework, sharp
details, cel-shaded flat colors with a limited palette (2-3 hard-edged
tonal steps per surface -- base, shadow, occasional highlight -- no soft
airbrushed gradients or blurry ambient occlusion). Clear legibility when
zoomed out to a small size. TONE EXCEPTION for this race: lean warm,
cozy, and inviting rather than the usual weathered/grim dark-fantasy
mood -- tidy, well-tended -- while staying in a muted, NOT
saturated-cartoon palette.

This asset must stay legible when shrunk down to a very small size, so:
use strong contrast between neighboring color regions; keep ornamental
detail minimal -- favor a small number of bold, simple shapes over fine
texture that won't survive downscaling anyway.

Subject: a ROUND, neatly-trimmed hedge mound (like a topiary ball),
used wherever the hedge bends, joins another stretch of hedge from any
direction, or stands alone -- dense living green hedge, seen from
directly above as a circle. A small charming bird bath or a modest
flowering bush sits atop the center of the mound as a homely accent.
This round shape is sized so its own outer curved edge touches all FOUR
sides of the square frame at once -- the circle's diameter equals the
full width (and height) of the tile, so the curve itself is tangent to
the top, bottom, left, and right edges simultaneously, EXACTLY at the
true pixel edge with no gap. CRITICAL sizing: draw this circle LARGE,
filling nearly the entire canvas with minimal padding -- do NOT draw a
smaller composition centered with lots of empty margin around it. A
small centered composition floating in a mostly-empty square frame is
WRONG for this asset. CRITICAL shape constraint: the silhouette is a
SINGLE CIRCLE and NOTHING ELSE -- do NOT add any straight rectangular
arms, stubs, spokes, or a plus/cross shape reaching toward the corners
or edges in addition to the circle. The four corner areas of the square
frame, between the circle's curve and the frame's actual corners, must
be empty background -- no hedge reaches into the corners at all.
Because the mound is round, a straight hedge segment can connect into
it seamlessly from ANY of the four cardinal directions. One small gold
(#c9a857) ribbon tied to the bird bath or bush as the only accent-color
element.

Framing: SQUARE 1:1 composition (equal width and height), matching one
terrain tile -- same convention as this game's road and river tile art,
not the taller portrait canvas used for standalone buildings, and the
SAME square aspect ratio as the straight horizontal/vertical wall
tiles. The round hedge mound is centered in the frame with its circular
edge touching all four sides as described; the bird bath/bush may rise
a little higher within this square frame than the straight segments'
trimmed top does, but must still read as clearly shorter than an
ordinary building's tallest form, let alone a city spire.

Background: a single flat, solid, saturated magenta (#FF00FF)
background, completely uniform, no gradient, no shadow cast onto it, no
vignette -- this will be chroma-keyed out, so nothing in the subject
itself should be magenta or near-magenta. Magenta MUST fill all four
corner areas outside the circle; only the circular hedge's curved edge
touches the frame, at exactly four tangent points (the edge midpoints).

Race material language: dense, neatly-trimmed living green hedge as the
dominant material, a small stone bird bath or flowering bush as the
central accent. The gold (#c9a857) accent color may only appear as the
small ribbon described above -- never as the dominant hue of the hedge.

Do not include any text, watermark, logo, UI elements, or border in the
image.
